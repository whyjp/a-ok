# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.eggs/

# Test/cache
.pytest_cache/
.coverage
htmlcov/

# Runtime data — runtime DB/logs/legacy dashboard HTML live OUTSIDE the repo
# at D:/work-github/.worker-control. These patterns are extra defense-in-depth
# in case someone copies data here.
*.sqlite3
*.sqlite3-journal
*.sqlite3-wal
*.sqlite3-shm
logs/
data/
.worker-control/
# Legacy `workerctl view html --legacy` output (NOT the packaged FE asset at
# worker_control/static/dashboard.html — that one must be tracked).
/dashboard.html
.playwright-mcp/

# Generated HTML reports (legacy-parity dashboard export target).
artifacts/

# OS
Thumbs.db
.DS_Store

# Editor
.idea/
.vscode/
